apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend-template.fullname" . }}
  labels:
    {{- include "backend-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend-template.selectorLabels" . | nindent 4 }}
