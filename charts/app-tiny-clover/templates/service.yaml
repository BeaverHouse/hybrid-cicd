apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-tiny-clover.fullname" . }}
  labels:
    {{- include "app-tiny-clover.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.api.port }}
      targetPort: api
      protocol: TCP
      name: api
    {{- if .Values.service.mcp.enabled }}
    - port: {{ .Values.service.mcp.port }}
      targetPort: mcp
      protocol: TCP
      name: mcp
    {{- end }}
  selector:
    {{- include "app-tiny-clover.selectorLabels" . | nindent 4 }}
