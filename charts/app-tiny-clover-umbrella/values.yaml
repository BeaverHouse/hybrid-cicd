# Umbrella chart values for tiny-clover applications
# Controls which services are enabled per cluster

services:
  file-manager:
    enabled: false
  llm-client:
    enabled: true
  search-manager:
    enabled: false
  smart-manager:
    enabled: false

# Values for each subchart when enabled
# These will use the original values from ../app-tiny-clover/values/
file-manager: {}
llm-client: {}
search-manager: {}
smart-manager: {}
